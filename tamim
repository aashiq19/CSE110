#include<stdio.h>

int main (){
    int x,y,n;
    scanf("%d%d%d",&x,&y,&n);
    if(3*x>y){
        int bulk=(n/3);
        int retl=n%3;
        int ans=(retl*x)+(bulk*y);
        printf("%d",ans);
    }
    else{
        printf("%d",x*n);
    }
    return 0;
}
